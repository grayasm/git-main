using System;

class Example
{
    static void Main()
    {
        int x, y;
        x = 100; y = 100;
        /*
            For example, the following two expressions are the same, but the second
            is easier to read:
        */

        x=10/y*(127+x);
        x = 10 / y * (127 + x);
    }
}