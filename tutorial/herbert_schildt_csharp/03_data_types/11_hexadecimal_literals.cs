using System;

class Hexadecimal
{
    static void Main()
    {
        int count = 0xFF; // 255 in decimal
        int incr = 0x1a; // 26 in decimal

        Console.WriteLine("count=" + count + " incr=" + incr);
    }
}