using System;

class Example
{
    static void Main()
    {
        /*
        The foreach loop cycles through the elements of a collection.
        A collection is a group of objects.
        C# defines several types of collections, of which one is an array.
        The foreach loop is examined in Chapter 7, when arrays are discussed.
        */
    }
}