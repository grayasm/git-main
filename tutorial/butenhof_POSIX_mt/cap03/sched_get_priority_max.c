/*
 * int  sched_get_priority_max(int policy)
 *
 *      The function shall return the appropriate maximum for scheduling policy
 *      specified by policy. The value of policy shall be one of the scheduling
 *      policy values defined in <sched.h>
 *
 *      If successful, it shall return the maximum value, otherwise it shall
 *      return -1 and set errno to indicate the error.
 */
