# BrandingText /TRIM(LEFT|RIGHT|CENTER) text
;
; Set the text at the bottom of the install window(default is 'Nullsoft Install System vX.XX')
; /TRIMLEFT trims down the size of the control to the size of the string.

BrandingText /TRIMRIGHT "Application Installer version 1.0"

Section
SectionEnd