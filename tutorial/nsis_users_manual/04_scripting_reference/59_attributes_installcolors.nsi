# InstallColors /windows | (foreground_color background_color)
;
; Sets the colors to use for the install info screen (the deafult is 00FF00 000000)
; Use the form RRGGBB (in hexadecimal ,as in HTML without #).
; Note that if /windows is specified as the only parameter, the default
; windows colors will be used.

InstallColors 000000 FFA500


Page directory
Page instfiles

Section ""
    DetailPrint "Installing ..."
SectionEnd
