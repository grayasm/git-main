; LicenseLangString  name  language_id | 0  license_path
;
; Does the same as LangString only it loads the string from a text/RTF file
; and defines a special LangString that can only be used by LicenseData.

; see 239_Instruction_LoadLanguageFile.nsi