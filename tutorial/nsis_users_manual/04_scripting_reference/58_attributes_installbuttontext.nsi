# InstallButtonText install_button_text
;
; If parameter is specified, overrides the default install button text "Install"
; with the specified text. Accepts variables.

InstallButtonText "Install (en)"

Page directory
Page instfiles

Section ""
    DetailPrint "Installing ..."
SectionEnd