; ${NSIS_PTR_SIZE}
;
; The size of a pointer (in bytes) in the generated installer.


!echo "The size of a pointer (in bytes) is ${NSIS_PTR_SIZE}"

; default
Section
SectionEnd

; compiler output:

;The size of a pointer (in bytes) is 4 