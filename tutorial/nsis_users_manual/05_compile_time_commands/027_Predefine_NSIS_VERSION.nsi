; ${NSIS_VERSION}
;
; NSIS version used to build the script.

!echo "NSIS version used to build the script is ${NSIS_VERSION}"

; default
Section
SectionEnd


; compiler output:

;NSIS version used to build the script is v3.06.1