; !undef  [/noerrors]  gflag  [...]
;
; Removes an item from the global define list. 
; Note that ${SYMBOL} where SYMBOL is undefined will be translated to "${SYMBOL}".

!define SOMETHING
!undef SOMETHING


; default
Section
SectionEnd