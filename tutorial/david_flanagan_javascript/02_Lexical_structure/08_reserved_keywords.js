/* Cap.2  Lexical Structure
   2.4.1. Reserved Words

   JavaScript reserves a number of identifiers as the keywords of the language
   itself. You cannot use these words as identifiers in your programs:

   break        delete        function        return        typeof
   case         do            if              switch        var
   catch        else          in              this          void
   continue     false         instanceof      throw         while
   debugger     finally       new             true          with
   default      for           null            try

   ECMAScript5 reserved but not used:
   class        const         enum            exports       extends
   import       super

   Reserved in Strict Mode:
   implements   let           private         public        yield
   interface    package       protected       static        arguments     eval


   ECMAScript3 reserved all the keywords of the Java language:
   abstract     double        goto            native        static
   boolean      enum          implements      package       super
   byte         export        import          private       synchronized
   char         extends       int             protected     throws
   class        final         interface       public        transient
   const        float         long            short         volatile


   JavaScript predefined global variables and functions:
   arguments    encodeURI     Infinity        Number        RegExp
   Array        encodeURIComponent            isFinite      Object      String
   Boolean      Error         isNaN           parseFloat    SyntaxError
   Date         eval          JSON            parseInt      TypeError
   decodeURI    EvalError     Math            RangeError    undefined
   decodeURIComponent         Function        NaN           ReferenceError
   URIError


   JavaScript implementations may define other global variables and functions.
   Each embedding JavaScript client-side,server-side will have its own list of
   global properties.
*/
