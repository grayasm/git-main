#!/usr/bin/env python
# -*- coding: utf-8 -*-

#
# The intertools Module
#
import itertools

#  chain(*iterables)
#  count(firstval=0)
#  cycle(iterable)
#  ifilter(func, iterable)
#  imap(func, *iterables)
#  islice(iterable[,start],stop[,step])
#  izip(*iterables)
#  repeat(items[,times])
#  tee(iterable, n=2)
